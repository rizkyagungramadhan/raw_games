import 'package:raw_games/utils/router/route_definition.dart';

class Routes {
  const Routes._();

  static const home = RouteDefinition(name: '/');
}

// TODO(agung): Create registration for each route here